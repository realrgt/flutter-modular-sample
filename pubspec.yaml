name: flutter_modular_sample
description: Deep dive into flutter modular

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  flutter_modular: ^2.0.1
  mobx: ^1.2.1+4
  flutter_mobx: ^1.1.0+2
  dio: ^3.0.10


  rx_notifier: ^0.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  build_runner: ^1.11.1
  mobx_codegen: ^1.1.2


flutter:

  uses-material-design: true
